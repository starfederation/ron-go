# math

Local copy of `github.com/delaneyj/geck/components/mathx` used by ron-go typed vocabularies.

The package is vendored as source under `components/math` to avoid adding the full `geck` module as a dependency while preserving the math type method sets used by math and spatial vocabularies.
